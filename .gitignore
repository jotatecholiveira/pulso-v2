# ============================================================
# Pulso — .gitignore
# ============================================================

# Config do app Android (contém apiKey + senderId + SHA-1)
android/app/google-services.json

# Node / build artifacts
node_modules/
dist/
*.log
